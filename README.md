# Evaluating-Models-Performance
This project evaluates the performance of a Logistic Regression classification model. I used customer features such as age, gender, country, and acquisition channel to predict customer loyalty tier. After preprocessing , I trained the model using training data and evaluated its performance using accuracy score and confusion matrix.
